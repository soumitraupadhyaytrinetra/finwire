---
import { siteConfig } from "../config";
import HeroSection from "../components/news/HeroSection.astro";
import NewsCard from "../components/news/NewsCard.astro";
import TrendingTopics from "../components/news/TrendingTopics.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { categories } from "../config";
import { getTopStories, getTrendingArticles, getArticlesByCategory, getTrendTopics, getAllArticles } from "../lib/data";

const topStories = getTopStories(15);
const trending = getTrendingArticles(4);
const marketsArticles = getArticlesByCategory("markets", 6);
const stocksArticles = getArticlesByCategory("stocks", 4);
const cryptoArticles = getArticlesByCategory("crypto", 4);
const fintechArticles = getArticlesByCategory("fintech", 4);
// The four category tiles below need the *real* total per category, not
// just the size of the limited preview fetch above (which is capped at 4).
// Compute counts by scanning the full article list.
const allArticles = getAllArticles();
const categoryTotal: Record<string, number> = {};
for (const a of allArticles) {
  categoryTotal[a.category] = (categoryTotal[a.category] || 0) + 1;
}
const trendTopics = getTrendTopics();
---

<BaseLayout title="Home">
  <HeroSection articles={trending.slice(0, 4).map(a => ({
    id: a.id,
    title: a.title,
    excerpt: a.excerpt,
    source: a.source,
    category: a.category,
    publicationDate: new Date(a.publicationDate),
    importanceScore: a.importanceScore,
    imageUrl: a.imageUrl,
  }))} />

  <div class="grid grid-cols-1 lg:grid-cols-[1fr_280px] gap-8">
    <div>
      <section class="mb-10">
        <h2 class="text-sm font-semibold tracking-wide uppercase text-[var(--text-tertiary)] mb-5">Top Stories</h2>
        <div class="space-y-3">
          {topStories.slice(0, 8).map((article) => (
            <NewsCard
              id={article.id}
              title={article.title}
              url={article.url}
              source={article.source}
              author={article.author}
              excerpt={article.excerpt}
              category={article.category}
              publicationDate={new Date(article.publicationDate)}
              importanceScore={article.importanceScore}
              imageUrl={article.imageUrl}
            />
          ))}
        </div>
      </section>

      {marketsArticles.length > 0 && (
        <section class="mb-10">
          <div class="flex items-center justify-between mb-5">
            <h2 class="text-sm font-semibold tracking-wide uppercase text-[var(--text-tertiary)] mb-0">Markets</h2>
            <a href="/markets" class="text-xs font-medium text-[var(--accent)] hover:underline">View all &rarr;</a>
          </div>
          <div class="space-y-3">
            {marketsArticles.map((article) => (
              <NewsCard {...article} publicationDate={new Date(article.publicationDate)} />
            ))}
          </div>
        </section>
      )}

      {stocksArticles.length > 0 && (
        <section class="mb-10">
          <div class="flex items-center justify-between mb-5">
            <h2 class="text-sm font-semibold tracking-wide uppercase text-[var(--text-tertiary)] mb-0">Stocks</h2>
            <a href="/stocks" class="text-xs font-medium text-[var(--accent)] hover:underline">View all &rarr;</a>
          </div>
          <div class="space-y-3">
            {stocksArticles.slice(0, 3).map((article) => (
              <NewsCard {...article} publicationDate={new Date(article.publicationDate)} />
            ))}
          </div>
        </section>
      )}

      {cryptoArticles.length > 0 && (
        <section class="mb-10">
          <div class="flex items-center justify-between mb-5">
            <h2 class="text-sm font-semibold tracking-wide uppercase text-[var(--text-tertiary)] mb-0">Crypto</h2>
            <a href="/crypto" class="text-xs font-medium text-[var(--accent)] hover:underline">View all &rarr;</a>
          </div>
          <div class="space-y-3">
            {cryptoArticles.slice(0, 3).map((article) => (
              <NewsCard {...article} publicationDate={new Date(article.publicationDate)} />
            ))}
          </div>
        </section>
      )}

      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mb-10">
        {categories.filter(c => ["banking", "forex", "economy", "regulation"].includes(c.id)).map((cat) => {
          const count = categoryTotal[cat.id] || 0;
          return (
            <a href={`/${cat.id}`} class="news-card p-6 text-center block hover:border-[var(--accent)] transition-colors">
              <h3 class="font-semibold text-base">{cat.label}</h3>
              <p class="text-xs text-[var(--text-tertiary)] mt-1">{count} articles</p>
            </a>
          );
        })}
      </div>
    </div>

    <aside class="max-lg:hidden">
      <div class="sticky top-24">
        <TrendingTopics trends={trendTopics.slice(0, 10)} period="trending" />
      </div>
    </aside>
  </div>
</BaseLayout>
